{
  "name": "extrovert-cafe",
  "private": true,
  "version": "0.0.1",
  "workspaces": ["api", "ws", "checkbox", "overseer", "pos", "client"],
  "scripts": {
    "up": "docker compose up -d",
    "down": "docker compose down",
    "logs": "docker compose logs -f --tail=100"
  }
}
