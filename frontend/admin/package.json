{
  "name": "@extrovert/admin",
  "private": true,
  "version": "0.0.1",
  "description": "Адмінка: статика на Cloudflare Workers, дані з api",
  "scripts": {
    "dev": "bun scripts/copy-kiosk-assets.mjs && bunx --bun vite",
    "build": "bun scripts/copy-kiosk-assets.mjs && bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "deploy": "bun run build && bunx wrangler@4 deploy",
    "deploy:dry": "bun run build && bunx wrangler@4 deploy --dry-run"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "@vitejs/plugin-react": "^4.3.4"
  }
}
