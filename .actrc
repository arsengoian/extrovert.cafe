# Образ раннера для act. Без цього рядка act на першому запуску питає його
# інтерактивно — і зависає в будь-якому скрипті.
-P ubuntu-latest=catthehacker/ubuntu:act-latest
# Порожнє оточення замість кореневого .env: інакше локальні змінні
# перекривають env воркфлоу (пояснення — у .github/act.env).
--env-file=.github/act.env
# Стільки образів одночасно. За замовчуванням act бере число ядер і
# піднімає сім задач разом — логін у ghcr із них тоді відвалюється по
# таймауту через одну-дві (23.09.2026: «dial tcp» і «context deadline
# exceeded» на випадкових сервісах, при тому що з хоста реєстр відповідає).
--concurrent-jobs 2
