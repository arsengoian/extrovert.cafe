# Питання по екранах і даних

Робочий файл: усе, чого не вдалось вивести з доків, дизайну чи коду під час
складання застосунку (20.09.2026). Кожен пункт — із тим, що я зробив
натомість, щоб ніщо не блокувало роботу. Відповіді зручно писати прямо тут
рядком «→».

---

## 1. Дизайн і доступ

1. **MCP дизайну не авторизується** в цій сесії (`/design-login` вимагає
   інтерактивного термінала). Обійшов: працюю з локальної копії
   `design/client/`, яку ти поклав. Питання: цю теку лишаємо поза git, як і
   решту `design/`? Тоді збірка клієнта залежить від неї лише на етапі
   копіювання ассетів — я вже скопіював потрібне в `client/public/assets`.

2. **Шрифт Extro** лежить в ассетах дизайну (`Extro400/600/700/900`). Ліцензія
   дозволяє віддавати його з вебзастосунку?

## 2. Економіка: чисел немає в доках

Порожні значення стоять `null` в `api/data/economy.json`, клієнт показує
«ціна уточнюється» замість вигаданого числа.

3. **Пачки догляду** — компост, добриво, інсектицид: ціна одного застосування
   в доках лише посиланням на XLSX (`§3.2`). Скільки коштує пачка з 3 одиниць?
4. **Крейт за гривню** — у доках «99-129 грн». Яке число ставимо?
5. **Набори монет** (§9.1): 100 / 500 / 1100 / 3000 монет — ціни в гривнях?
6. **Квізи**: скільки монет дає анкета профілю і скільки квіз про напій?
7. **Репост**: скільки монет за підтверджений репост?
8. **Ціни в зернах** для кави, чашки й футболки в UI приблизні («~30», «~15»,
   «36-45»). Зафіксувати точні?
9. **Курс зерна** — 13-17 монет. Поки взяв 15 (середина). Ок?

## 3. Дані, які розходяться між собою

10. **Ціни напоїв vs монети за них.** `pos/data/prices.json` (жива ціна) і
    таблиця маржі в `gamification_economy.md` §7 розходяться: еспресо 35 проти
    30, мокачино 45 проти 55, флет-вайт 55 проти 50, ірландський 50 проти 55.
    Монети в сіді взяв із таблиці §7 (це рішення), ціни — з prices.json
    (це прод). Перерахувати монети під нові ціни?
11. **Бонус-напої `x034`/`x038`** є в меню й у сіді `drinks.json`, але в
    каталозі Checkbox їх немає (`checkbox.md`). Заводимо в кабінеті?
12. **Адреса точки** `kyiv-01` у сіді порожня — я не знайшов її в доках.

## 4. Кавенятко й спрайти

13. **Набори облич.** У схемі `face_set_id` — число, у графіці описано 5
    наборів, а намальований поки один (`face_setA_*`). Поки рендерю набір A
    для будь-якого `face_set_id`. Коли зʼявляться інші — мапа «id → тека»?
14. **Тіло за стадіями.** Є два спрайти: `body_stage0_sprout` і
    `body_stage1_with_face_slot`. Стадії 2-10 — це та сама сфера плюс листя,
    гілки й плоди? Зараз роблю саме так.
15. **Що кущ хоче на якій стадії** я вивів із таблиці переходів в UI-доці
    (компост 1→3, добриво 3→7, далі інсектицид). Хмаринка показує саме це.
    Звірити?
16. **Позиції листя/гілок.** У стані рослини є `x/y`, але дизайн малює
    композицію, підігнану вручну. Поки розкладаю за `x/y` з JSON. Чи є
    канонічні зони з макета, які треба зашити?

## 5. Вхід і акаунти

17. **Google/Apple** ще не підключені. Поставив девелоперський вхід
    (`POST /api/v1/auth/dev`, лише коли `DEV_TOOLS=1`/не прод). Коли
    підключаємо справжній OAuth — і чий саме клієнт (окремий проєкт у Google
    Cloud на наш домен)?
18. **Нікнейм** автогенерується як `слово-1234` (кавові слова). Формат ок?

## 6. Екрани, які ще не зроблені

Каркас, дані й навігація готові; ці екрани з дизайну лишились на чергу —
кожен потребує або рішення вище, або ще одного роуту:

- квізи (анкета профілю 5 кроків, квіз про напій);
- посадка листя/гілок/бутонів (6 екранів) — потрібні канонічні зони (п. 16);
- гардероб і примірочна, чат із кавенятком;
- чекаут із Новою Поштою (вибір міста й відділення, таблиця розмірів);
- P2P: продаж одягу й кавенятка, «на продажу», зняття з продажу;
- скринька: анімація відкриття, «предмет зі складу», «кому вдягнути»;
- редім бонусу (сканування QR з кіоска), «не вистачає монет/препарату»;
- профіль, зміна нікнейма, підтримка, умови й приватність.

## 7. Дрібне, але заважатиме

19. **Лічильник «Мої замовлення»** — це кількість замовлень у дорозі чи
    кількість непереглянутих змін статусу? Зараз рахую друге
    (`user_seen_at < status_changed_at`).
20. **Світла тема** в дизайні є (`data-theme="light"`). Перемикач у
    застосунку буде, чи йдемо за системною темою?
21. **Локальний стек** зайняв порти 5442/6389 замість 5432/6379 — у тебе вже
    крутиться інший постгрес і редіс. Лишаю так?
