/* selftest.h — "чи цей бінарник узагалі намалює екран", без дисплея.
 *
 * Навіщо окремий режим, а не просто запустити нову версію й подивитись:
 * dispmanx-шар кіоска може належати лише ОДНОМУ процесу, а на момент
 * перевірки старий кіоск ще працює й показує меню. Тобто "перевірити
 * оновлення перед підміною" і "запустити оновлення" — фізично несумісні,
 * якщо перевірка потребує екрана. Тому selftest не чіпає platform_* взагалі:
 * лише Cairo/Pango/librsvg у памʼять і перевірка результату.
 *
 * Ловить рівно ті поломки, через які кіоск після оновлення показав би
 * чорний екран, і які видно без дисплея: неповний архів (нема шрифту чи
 * шаблону), битий SVG, незлінкована бібліотека, падіння на старті.
 * НЕ ловить: помилки самого GL-композитора й dispmanx — їх перевіряє вже
 * health-check після підміни (pi/stack/updater.sh), з відкатом.
 */
#ifndef POS_NATIVE_SELFTEST_H
#define POS_NATIVE_SELFTEST_H

/* 0 — усе добре, 1 — щось не так (деталі в stderr). out_png може бути NULL:
 * тоді результат нікуди не зберігається, перевіряється тільки в памʼяті. */
int selftest_run(const char *assets_dir, const char *out_png);

#endif
